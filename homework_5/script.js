//Напишите функцию random(min, max), 
//которая будет возвращать случае целое число от min до max
// function random(min, max) {
// 	let result;
// 	if (isNaN(min) && isNaN(max)) {
// 		alert('Неверное число');
// 		return;
// 	}
// 	return Math.floor((max - min) * Math.random() + min);
// }
// function call_random() {
// 	min = +(prompt('min'));
// 	max = +(prompt('max'));
// 	for (let i = 0; i < 5; i++) {
// 		console.log(random(min, max));
// 	}
// }




// Написать функцию count_symbol( symbol, string ), 
// которая подсчитает количество указанного символа symbol,
// в строке string.
// Функция должна быть регистронезависимой и вернуть число


function count_symbol(symbol, string) {
	
}